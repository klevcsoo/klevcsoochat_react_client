import React from 'react'

const AppSubtitle = ({ text }) => {
  return (
    <h1 style={{
      fontSize: 20,
      fontWeight: 300,
      opacity: 0.8,
      textAlign: 'center'
    }}>{text}</h1>
  )
}

export default AppSubtitle
