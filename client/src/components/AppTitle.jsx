import React from 'react'

const AppTitle = ({ text }) => {
  return (
    <h1 style={{
      fontFamily: '"Rubik", sans-serif',
      fontSize: 40,
      fontWeight: 700
    }}>{text}</h1>
  )
}

export default AppTitle
